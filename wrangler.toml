# Cloudflare Pages configuration for SodCalc
# Auto-deploys when pushed to main branch
name = "sodcalc"
pages_build_output_dir = "src/dist"
compatibility_date = "2026-06-11"

# Custom domain:
# Cloudflare Dashboard > Workers & Pages > sodcalc > Custom Domains > Add sodcalc.com
# DNS will be auto-configured